enum ActionType {
  NORMALIZE_SWEETS_ITEM = '@normalize/SWEETS_ITEM',
  NORMALIZE_SHOP = '@normalize/SHOP',
  NORMALIZE_LARGE_CATEGORY = '@normalize/LARGE_CATEGORY',
  NORMALIZE_SMALL_CATEGORY = '@normalize/SMALL_CATEGORY',
}

export default ActionType;
